# ── Core Backend (PINNED — must be compatible) ──────────
fastapi==0.115.2
uvicorn==0.34.0
starlette==0.40.0
pydantic==2.12.0
pydantic-settings==2.12.0
httpx==0.28.1
python-multipart==0.0.18

# ── PDF Parsing (PINNED) ────────────────────────────────
PyMuPDF==1.24.0
pdfplumber==0.11.0
Pillow==10.4.0

# ── Database ────────────────────────────────────────────
neo4j==5.23.0

# ── Tunnel ──────────────────────────────────────────────
pyngrok

# ── AI / ML (unpinned — Colab provides torch) ──────────
transformers
accelerate
torch
